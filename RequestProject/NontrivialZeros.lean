import Mathlib
import RequestProject.Defs

/-!
# Nontrivial Zeros of the Riemann Zeta Function

Re-exports from `Defs.lean` for backward compatibility.
All definitions now use `UNIT` and `UNIT / 2` as the
fundamental parameters, making the connection to scaled
coordinate systems (e.g. log(7), log(7)/2) transparent.

See `Defs.lean` for the primary definitions.
-/

-- All definitions are now in Defs.lean.
-- This file exists for backward compatibility with any external imports.
